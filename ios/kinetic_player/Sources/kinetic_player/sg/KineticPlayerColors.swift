import UIKit

/// Shared seek-bar colors aligned with Android `kinetic_seek_progress`.
enum KineticPlayerColors {
    static let seekActive = UIColor(red: 0.30, green: 0.91, blue: 0.71, alpha: 1)
    static let seekBackground = UIColor(white: 1, alpha: 0.35)
    static let panelBackground = UIColor(white: 0, alpha: 0.6)
}
